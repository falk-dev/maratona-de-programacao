#include <bits/stdc++.h>

using namespace std;

#define dbvector(vector)                           \
    {                                              \
        cout << #vector << " = ";                  \
        for (auto& it : vector) cout << it << " "; \
        cout << endl;                              \
    }

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    return 0;
}